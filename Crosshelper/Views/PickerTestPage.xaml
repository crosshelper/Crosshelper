<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sv="clr-namespace:AiForms.Renderers;assembly=SettingsView"
             x:Class="Crosshelper.Views.PickerTestPage">
    <ContentPage.Content>
        <StackLayout>
            <sv:SettingsView VerticalOptions="Center">
                <sv:Section Title="Section">
                    <sv:TextPickerCell Title="TextPickerCell" Items="{Binding Items}" />
                    <sv:PickerCell Title="PickerCell" ItemsSource="{Binding ItemsSource}" SelectedItems="{Binding SelectedItems}" DisplayMember="Name"/>
                </sv:Section>
            </sv:SettingsView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>